<Window x:Class="RobotSharpV2.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:RobotSharpV2"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Image x:Name="CameraImage" Grid.Row="0" Grid.Column="0" Stretch="Uniform"/>
        <Image x:Name="MaskImage" Grid.Row="0" Grid.Column="1" Stretch="Uniform" Margin="10"/>
        <Slider x:Name="BinarizationSlider" Grid.Row="1" Grid.Column="1" 
                Minimum="0" Maximum="255" Value="100" TickFrequency="5"
                IsSnapToTickEnabled="True" VerticalAlignment="Center"
                Width="200" Margin="181,0,19,0" 
                ValueChanged="BinarizationSlider_ValueChanged"/>
        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Center" Margin="10">
            <Button x:Name="StartStopButton" Content="СТАРТ" 
                 Click="StartStopButton_Click" Width="100"/>
            <TextBlock x:Name="GestureStatus" Foreground="DarkRed" FontSize="16"
                    Margin="10" VerticalAlignment="Center"/>
            <TextBlock x:Name="FrameRate" Foreground="Blue" FontSize="14"
                    Margin="10" VerticalAlignment="Center"/>
        </StackPanel>
    </Grid>
</Window>
